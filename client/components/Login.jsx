const React = require('react');
import { render } from 'react-dom';
import { useState } from 'react';

const Login = (props) => {
  return (
    <div>
      HELLO
      <input></input>
      <input></input>
    </div>
  );
};

export default Login;
